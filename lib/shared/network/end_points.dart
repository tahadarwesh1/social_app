const LOGIN = 'login';

const REGISTER = 'register';

const LOGOUT = 'logout';

const SEARCH = 'products/search';

const UPDATE = 'update-profile';

const HOME = 'home';

const GET_CATEOGRIES = 'categories';

const FAVORITES = 'favorites';

const PROFILE = 'profile';
